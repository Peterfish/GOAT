[build-system]
requires = [
    "setuptools",
    "wheel",
    "setuptools-scm",
]

[tool.setuptools]
packages = ["goat_storytelling_agent"]

[project]
name = "goat_storytelling_agent"
version = "0.0.1"
dependencies = [
    "ollama>=0.4.0"
]